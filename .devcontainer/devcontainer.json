{
  "name": "codespace with docker",
  // Install Docker (dind) inside the devcontainer
  "features": {
    "ghcr.io/devcontainers/features/docker-in-docker:2": {
      "version": "latest"
    }
  },

  // (Optional but helpful) make sure the container has enough oomph
  "hostRequirements": { "cpus": 4, "memory": "8gb" },

  // Don't probe Docker here—daemon may not be ready yet.
  "postCreateCommand": "echo 'Devcontainer created ✅'",

  // Wait until dockerd is up, then compose up
  "postStartCommand": "bash -lc 'until docker info >/dev/null 2>&1; do echo waiting for docker...; sleep 2; done; docker version; (docker compose version || docker-compose --version || true); docker compose pull || true; docker compose up -d'",

  // Forward whatever ports your services expose (example: 5432)
  "forwardPorts": [8888, 5050, 5432]
  
}
